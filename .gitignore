# ── F6222 driver build ──────────────────────────────────────────
libf6222.a
src/*.o
src/*.d

# ── Generic C/C++ build artefacts ───────────────────────────────
*.o
*.d
*.a
*.so
*.su
*.cyclo

# Firmware images / link maps
*.elf
*.bin
*.hex
*.map
*.lst
*.list

# ── STM32 CubeMX / CubeIDE ─────────────────────────────────────
# Makefile generator output (see BUILD_DIR in project Makefile)
build/
Debug/
Release/

# STM32CubeIDE / Eclipse (if project is opened in IDE later)
.settings/
.project
.cproject
*.launch

# CubeMX local backup (regenerated on save)
*.ioc.bak

# ── Editor / tooling ────────────────────────────────────────────
.cache/
*.swp
*~

# ── OS junk ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
